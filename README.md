# Inventory Management System
This project is a C-based system that manages inventory using a **Binary Search Tree (BST)** for efficient data handling.

## Features
- Add, delete, and search for products.
- Uses BST for O(log n) average time complexity.
- Tracks product IDs, names, prices, and quantities.
